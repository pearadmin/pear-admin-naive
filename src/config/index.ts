export const DEFAULT_THEME = 'auto'

export const DEFAULT_STORAGE = sessionStorage

export const DEFAULT_TABLE_SIZE = 'medium'

export const DEFAULT_TABLE_HEIGHT = 600

export const DEFAULT_TABLE_FETCH = {
  method: 'post',
  bodyType: 'data'
}

export const TABLE_PAGINATION = {
  pageSize: 'pageSize',
  pageNo: 'pageNo',
  total: 'total'
}

export const TABLE_FETCH_RESPONSE = {
  pageSize: 'pageSize',
  pageNo: 'pageNo',
  total: 'total',
  list: 'list'
}
